rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Protótipo sem login: o acesso aberto expira automaticamente em 1º de outubro.
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 10, 1);
    }
  }
}
